# Core dependencies
# （プロファイラーはPure Pythonで実装するため、外部依存は最小限）

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
mypy>=1.0.0
flake8>=6.0.0

# Optional dependencies for enhanced features
# psutil>=5.9.0  # システムリソース計測用（オプション）
